export enum ErrorCodes {
  /**
   * In the event of hitting an endpoint from the server that times out
   * because of unforeseen circumstances, you can log an error using this
   * code
   */
  SERVER_TIMEOUT = 1001,

  //Feel free to add more error codes to define issues in explicit use cases
}
